version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0.9
    steps:
      - checkout
      - run:
          name: Run maven build
          command: mvn clean install
      - run: mkdir artifacts
      - run: cp */target/*shaded*.jar artifacts
      - store_artifacts:
          path: artifacts
